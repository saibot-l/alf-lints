name: alf_lints
description: Opinionated Dart lints to maintain a clean application structure.
version: 1.0.0
repository: https://github.com/saibot-l/alf-lints

environment:
  sdk: ^3.3.1

dependencies:
  analyzer:
  analyzer_plugin:
  custom_lint_builder:

dev_dependencies:
  lints: ^3.0.0
